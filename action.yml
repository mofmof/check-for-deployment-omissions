name: 'check deployment ommissions'
description: 'Check for Deployment Omissions'
author: 'mofmof'
inputs:
  github_token:
    required: true
    description: 'GitHub Token'
  slack_token:
    required: true
    description: 'Slack Token'
  slack_channel:
    required: true
    description: 'Slack Channel'
runs:
  using: 'node16'
  main: 'dist/index.js'
